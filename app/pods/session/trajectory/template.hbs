{{#if model.disabled}}
  <div class="error-pane">
    <div class="error-title">
      Evolutionary data not available
    </div>
    <div class="error-message">
      This analysis was disabled in the pipeline.
    </div>
  </div>
{{else}}
  <div class="form-inline">
    <div class="form-group">
      <label for="evo_regions">Region(s) to plot</label>
    </div>
    <div class="form-group">
      {{#power-select-multiple
	 options=regions
	 selected=selectedRegions
	 searchPlaceholder="Select region(s)"
	 searchField='name'
	 onchange=(action (mut selectedRegions))
	 as |region|
      }}
	{{region.name}}
      {{/power-select-multiple}}
    </div>

    <div class="form-group">
      <label for="evo_metrics">Evolutionary metrics</label>
    </div>
    <div class="form-group">
      {{#power-select-multiple
	 options=evoMetrics
	 selected=selectedEvoMetrics
	 searchPlaceholder="Select metric(s)"
	 searchField='name'
	 onchange=(action (mut selectedEvoMetrics))
	 as |evoMetric|
      }}
	{{evoMetric.name}}
      {{/power-select-multiple}}
    </div>

    {{#if firstEvoRegion}}
      <div class="form-group">
	<label class="text-error">(Only showing {{firstEvoRegion}})</label>
      </div>
    {{/if}}

  </div>

  <div class = 'row'>
    <div class = 'col-md-12' id="_timeseries_plot">
      {{timeseries-plot data=evoData ymin=0 tickMap=model.dates}}
    </div>
  </div>

  <div class="form-inline">
    <div class="form-group">
      <label for="_pheno_metrics">Phenotype metrics</label>
    </div>
    <div class="form-group">
      {{#power-select-multiple
	 options=phenoMetrics
	 selected=selectedPhenoMetrics
	 searchPlaceholder="Select metric(s)"
	 searchField='name'
	 onchange=(action (mut selectedPhenoMetrics))
	 as |phenoMetric|
      }}
	{{phenoMetric.name}}
      {{/power-select-multiple}}
    </div>
    {{#if firstPhenoRegion}}
      <div class="form-group">
	<label class="text-error">(Only showing {{firstPhenoRegion}})</label>
      </div>
    {{/if}}
    {{#if excludedPhenoMetric}}
      <div class="form-group">
	<label class="text-error">(Not showing {{excludedPhenoMetric}})</label>
      </div>
    {{/if}}
  </div>

  <div class = 'row'>
    <div class = 'col-md-12' id="_timeseries_plot">
      {{timeseries-plot data=phenoData data2=phenoData2 tickMap=model.dates}}
    </div>
  </div>
{{/if}}
