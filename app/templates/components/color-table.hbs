<table class="table table-hover table-condensed">
  <thead>
    <tr>
      {{#each h in header}}
      <td>{{h}}</td>
      {{/each}}
    </tr>
  </thead>
  <tbody>
    {{#each row in data}}
    <tr>
      {{#each elt in row}}
      <td {{bind-attr style=elt.style}} {{bind-attr title=elt.title}}>
        {{elt.value}}
      </td>
    {{/each}}
    </tr>
    {{/each}}
</table>
