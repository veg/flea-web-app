<div class="input-group-btn">
  <button {{action "moveRanges" -5}} type="button" class="btn btn-default btn-sm" title="Jump left">
    <i class="fa fa-fast-backward" ></i>
  </button>
</div>
<div class="input-group-btn">
  <button {{action "moveRanges" -1}} type="button" class="btn btn-default btn-sm" title="Move left">
    <i class="fa fa-backward" ></i>
  </button>
</div>
<span class="input-group-addon">Range</span>
{{input type="string" class="form-control" value=rangeText
  placeholder="HXB2 coordinate range (e.g. 160-200)"
  autofocus="autofocus" focus-out="display" insert-newline="display"
}}
<div class="input-group-btn">
  <button {{action "moveRanges" 1}} type="button" class="btn btn-default btn-sm" title="Move right">
    <i class="fa fa-forward" ></i>
  </button>
</div>
<div class="input-group-btn">
  <button {{action "moveRanges" 5}} type="button" class="btn btn-default btn-sm" title="Jump right">
    <i class="fa fa-fast-forward" ></i>
  </button>
</div>
{{#if predefinedRegions}}
<div class="input-group-btn">
  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">Predefined
    <span class="caret"></span></button>
  <ul class="dropdown-menu">
    {{#each region in predefinedRegions}}
    <li>
      <a {{action "setRange" region.start region.stop}} class="seq-predef">
        {{region.name}}
      </a>
    </li>
    {{/each}}
  </ul>
</div>
{{/if}}
